package xyz.cybros.magic8ball;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    /* Add declarations for objects(Button, TextView, EditText, etc.)
     * and primitive variables(int, float, etc.) here
     * e.g.
     * TextView textView;
     */

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        /* Initialize the variables and objects here
         * e.g.
         * textView = (TextView) findViewById(R.id.textView);
         * (set the id as textView in activity_main.xml)
         */
    }
}
